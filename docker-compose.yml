version: '3.5'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: api
    volumes:
      - type: volume
        source: venv
        target: /app/.venv
    ports:
      - '3002:5000'
    env_file: .env

volumes:
  venv:
